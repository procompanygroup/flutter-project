class HomeDetails {
  final String model, name, period, description, developedby, cost;
  final int lat, log;

  HomeDetails(
      {required this.model,
      required this.name,
      required this.period,
      required this.description,
      required this.developedby,
      required this.cost,
      required this.lat,
      required this.log});
}
